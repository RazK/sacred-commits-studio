node_modules/
dist/

# Sefaria API cache — fetched at build time by Vercel (npm run fetch)
# Large aggregate files not needed in repo
data/sefaria/sukkah-all.json
data/sefaria/sukkah-index.json

# Generated mock-repo data — regenerate with npm run generate
data/mock-repo/commits.json
data/mock-repo/diffs.json
data/mock-repo/stats.json
